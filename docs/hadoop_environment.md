# Hadoop Environment

The analytics workflow was designed to execute within an enterprise
Hadoop environment.

Workflow:

PowerShell Logs → HDFS → Python Analytics → Feature Engineering →
Regression → Reporting

Using Hadoop allowed large daily log volumes to be processed efficiently
while supporting future scalability.
